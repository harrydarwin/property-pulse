// 2. house, buidling or address,
// if house take address, if building take address, if radius take address and radius - eventually show map
import React from 'react'

const QueryLocation = () => {
  return (
    <div>QueryLocation</div>
  )
}

export default QueryLocation